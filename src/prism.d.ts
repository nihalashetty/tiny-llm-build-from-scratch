// Prism's per-language component files ship no type declarations. We import
// them only for their side effect (registering a grammar), so an empty module
// declaration is all TypeScript needs.
declare module 'prismjs/components/*';
